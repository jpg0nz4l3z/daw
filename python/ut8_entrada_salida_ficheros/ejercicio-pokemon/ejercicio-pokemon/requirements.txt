#
# Para crear un entorno virtual (opcional pero recomendado):
#   python -m venv .venv
# Activarlo en Windows:
#   .venv\Scripts\activate
# En macOS/Linux:
#   source .venv/bin/activate
# Para instalar dependencias:
#   pip install -r requirements.txt
#
# Este ejercicio solo usa la libreria estandar (json, os, collections),
# por lo que no se requieren paquetes externos.
